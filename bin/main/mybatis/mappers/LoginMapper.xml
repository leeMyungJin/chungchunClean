<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chungchunClean.Mappers.LoginMapper">

	<select id="getPassword" parameterType="StaffVo" resultType="StaffVo">
		select
			staff_id,
			staff_pass,
			password_key
		from web.staff
		where staff_id = #{id}
	</select>
	
	<update id="updateLoginTime" parameterType = "StaffVo">
		update web.staff set
		lateass_dt = #{lastLogin}::timestamp
		where staff_id = #{id}
	</update>
	
	<select id="getUserToken" parameterType="hashMap" resultType="String">
		select usertoken from web.staff where staff_id = #{id}
	</select>
	
</mapper>